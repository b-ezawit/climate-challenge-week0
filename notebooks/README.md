# Notebooks Directory

This folder is dedicated to Jupyter Notebooks used for:
* **Exploratory Data Analysis (EDA)**: Initial investigation of the climate dataset.
* **Visualization**: Plotting trends and distributions.
* **Prototyping**: Testing logic before moving it into the `src/` directory.

> **Note:** Ensure that `.ipynb_checkpoints/` is ignored by Git to keep the repository clean.
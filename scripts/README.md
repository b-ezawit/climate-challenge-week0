# Scripts Directory

This folder contains standalone Python scripts that are not part of the core package logic. This includes:
* **Data Fetching**: Scripts to download or update raw data.
* **Preprocessing**: Automation for cleaning or transforming large datasets.
* **Environment Verification**: Simple scripts to check if dependencies are correctly installed.

To run a script from this folder, use:
`python scripts/your_script_name.py`